import Header from "../components/Header";

const NotFoundPage = () => {
    return (
        <div>
            <Header buttonText="Log in" />
            <h1>It looks like this page does not exist - sorry about that</h1>
        </div>
    );
};

export default NotFoundPage;
